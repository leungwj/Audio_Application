export type User = {
  id: string;
  username: string;
  email: string;
  full_name: string;
  disabled: boolean;
};

export type Audio_File = {
  id: string;
  description: string;
  category: string;
}